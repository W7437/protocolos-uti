# ProtocolosUTI

Esta é a pasta substituível do site.

Nas próximas versões, substitua apenas esta pasta `ProtocolosUTI/` inteira.
Os arquivos `wrangler.toml` e `package.json` ficam na raiz do repositório e não precisam ser alterados.

Estrutura esperada:

ProtocolosUTI/
└── site/
    ├── index.html
    ├── styles.css
    └── app.js
